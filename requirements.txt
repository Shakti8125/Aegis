# Aegis - pinned dependencies.
# Phase 1 (simulator/) needs only these four; they install from prebuilt
# wheels on Python 3.13 with no compilation step.
numpy==2.5.1
gymnasium==1.3.0
pettingzoo==1.26.1
pytest==9.1.1

# Phase 2 (graph/). Official Neo4j bolt driver - no APOC, no HTTP, so the same
# code runs against the local neo4j:5-community container and AuraDB Free.
# tests/graph/ needs a reachable database and skips itself when there is none.
neo4j==6.2.0

# Phases 3-4 (encoder/, marl/). CPU wheels are enough: MAPPO here trains on a
# 12-agent, 38-float observation space, so the run is bounded by the simulator
# rather than by matmul throughput. torch-geometric is a Phase 3 dependency;
# marl/ imports only torch (marl/mappo.py's encoder seam keeps the GNN optional).
torch==2.9.1
torch-geometric==2.8.0.post1

# Phase 6 (backend/ and ops_layer/) dependencies
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
